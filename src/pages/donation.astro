---
import Layout from "../layouts/Layout.astro";
import QRCodeImage from "../assets/qr-code.webp";
import { Image } from "astro:assets";
---

<Layout title="Welcome to Astro.">
  <section class="px-5 py-16 md:py-24 bg-dim">
    <div class="max-w-[840px] w-full mx-auto gap-6 flex flex-col">
      <p class="md:text-2xl font-semibold text-accent-secondary">Donate</p>
      <h1 class="text-2xl md:text-4xl lg:text-6xl font-bold">
        Partner with Us to Build a Better World
      </h1>
      <p class="md:text-2xl/normal">
        Your generosity will help us bring diverse communities together and
        foster understanding and harmony, while promoting awareness and growth.
        Thank you for joining us in our mission to create a more inclusive and
        harmonious community.
      </p>
    </div>
  </section>

  <section class="px-5 py-16">
    <div class="max-w-[840px] mx-auto flex flex-col lg:flex-row gap-10 items-center">
      <div class="flex flex-col items-center justify-center gap-5 flex-1">
        <p class="font-semibold">If you prefer to give by cheque</p>
        <div class="p-5 rounded-md bg-dim">
          <div class="w-full flex items-center justify-between gap-8">
            <p class="opacity-60">Account Name:</p>
            <p class="font-medium text-right">EERF Foundation</p>
          </div>

          <div class="w-full flex items-center justify-between gap-8">
            <p class="opacity-60">Account #:</p>
            <p class="font-medium text-right">4090 9097 134309</p>
          </div>

          <div class="w-full flex items-center justify-between gap-8">
            <p class="opacity-60">Bank:</p>
            <p class="font-medium text-right">ICICI Bank</p>
          </div>

          <div class="w-full flex items-center justify-between gap-8">
            <p class="opacity-60">IFSC Code:</p>
            <p class="font-medium text-right">ICIC000909</p>
          </div>
        </div>
      </div>

      <div
        class="text-sm uppercase tracking-wider relative font-medium opacity-40 self-stretch flex items-center justify-center"
      >
        <div
          class="absolute w-px h-full inset-y-0 bg-black/10 left-1/2 -translate-x-1/2"
        >
        </div>
        <div class="text-sm uppercase tracking-wider bg-white relative">Or</div>
      </div>

      <div class="flex flex-col items-center justify-center gap-5 flex-1">
        <p class="font-semibold">Scan QR Code to donate us</p>
        <div class="p-2 rounded-md bg-accent-secondary">
          <Image
            class="w-[120px] h-[120px]"
            src={QRCodeImage}
            width={360}
            alt={"City at night"}
          />
        </div>
      </div>
    </div>
  </section>
</Layout>
