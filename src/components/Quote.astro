---
import { Icon } from "astro-icon";
---

<section class="px-5 py-32 gradient-split">
  <div
    class="flex flex-col gap-6 items-center justify-center text-center shadow-lg bg-white rounded-lg p-16 max-w-[840px] mx-auto"
  >
    <div
      class="w-12 h-12 bg-accent-primary grid place-items-center text-white rounded-full"
    >
      <Icon name="quote" size={24} />
    </div>
    <h2 class="text-3xl font-bold">
      The best way to find yourself is to lose yourself in the service of
      others.
    </h2>
    <p class="text-accent-secondary">— Mahatma Gandhi</p>
  </div>
</section>

<style>
  .gradient-split {
    background: linear-gradient(
      180deg,
      rgba(var(--color-dim), 1) 0%,
      rgba(var(--color-dim), 1) 50%,
      rgba(var(--color-accent-primary), 1) 50%,
      rgba(var(--color-accent-primary), 1) 100%
    );
  }
</style>
